# سیستم داینامیک متا برای فرم المنتور

یک سیستم قدرتمند و کاملاً داینامیک برای ذخیره خودکار فیلدهای فرم المنتور به عنوان متا در جاهای مختلف وردپرس.

## 📋 فهرست مطالب

- [ویژگی‌ها](#ویژگی‌ها)
- [نصب](#نصب)
- [نحوه استفاده](#نحوه-استفاده)
- [انواع متا](#انواع-متا)
- [مثال‌های کاربردی](#مثال‌های-کاربردی)
- [نکات مهم](#نکات-مهم)
- [عیب‌یابی](#عیب‌یابی)

## ✨ ویژگی‌ها

- ✅ کاملاً داینامیک و بدون نیاز به کدنویسی اضافی
- ✅ پشتیبانی از User Meta
- ✅ پشتیبانی از Post Meta (برای همه پست تایپ‌ها)
- ✅ پشتیبانی از Product Meta (ووکامرس)
- ✅ پشتیبانی از Term Meta (دسته‌بندی و تگ)
- ✅ پشتیبانی از Comment Meta
- ✅ امکان استفاده از ID ثابت یا داینامیک
- ✅ Sanitize خودکار داده‌ها
- ✅ سبک و بدون سربار اضافی

## 🚀 نصب

### مرحله 1: کپی کردن کد
کد PHP موجود در فایل را کپی کرده و در انتهای فایل `functions.php` قالب خود قرار دهید:

```php
// مسیر فایل قالب
wp-content/themes/YOUR-THEME/functions.php
```

### مرحله 2: ذخیره و تست
فایل را ذخیره کنید و سایت خود را رفرش کنید. سیستم به صورت خودکار فعال می‌شود.

## 📖 نحوه استفاده

### قدم اول: نام‌گذاری فرم
نام فرم المنتور خود را طوری انتخاب کنید که با `-meta` پایان یابد:

```
نام فرم: contact-form-meta
نام فرم: user-profile-meta
نام فرم: product-update-meta
```

### قدم دوم: نام‌گذاری فیلدها
شناسه (ID) فیلدهای فرم را بر اساس الگوی زیر تنظیم کنید:

```
[نام-متا]-meta-[نوع]-[آیدی]
```

## 🎯 انواع متا

### 1. متای کاربر (User Meta)

برای ذخیره در متای کاربر فعلی:

```
شناسه فیلد: phone-meta-user
شناسه فیلد: address-meta-user
شناسه فیلد: company-meta-user
```

**نتیجه:**
- `phone` در `user_meta` کاربر فعلی ذخیره می‌شود
- اگر متا وجود نداشته باشد، ایجاد می‌شود
- اگر موجود باشد، به‌روزرسانی می‌شود

---

### 2. متای محصول (Product Meta)

#### 2.1. با ID ثابت:
```
شناسه فیلد: discount-meta-product-123
شناسه فیلد: stock-meta-product-456
```

**نتیجه:** در متای محصول با ID مشخص ذخیره می‌شود

#### 2.2. با ID داینامیک:
```
فیلد 1 شناسه: product_id (نوع: Hidden یا Number)
فیلد 2 شناسه: discount-meta-product
فیلد 3 شناسه: warranty-meta-product
```

**نتیجه:** 
- سیستم ID را از فیلد `product_id` می‌خواند
- متاها در محصول مربوطه ذخیره می‌شوند

---

### 3. متای پست (Post Meta)

#### 3.1. با ID ثابت:
```
شناسه فیلد: views-meta-post-789
شناسه فیلد: rating-meta-post-100
```

#### 3.2. با ID داینامیک:
```
فیلد 1 شناسه: post_id
فیلد 2 شناسه: custom-field-meta-post
```

---

### 4. متای پست تایپ دلخواه

برای هر پست تایپی که در وردپرس دارید (مثل book، course، event):

#### 4.1. با ID ثابت:
```
شناسه فیلد: author-meta-book-50
شناسه فیلد: instructor-meta-course-25
شناسه فیلد: location-meta-event-99
```

#### 4.2. با ID داینامیک:
```
فیلد 1 شناسه: book_id
فیلد 2 شناسه: genre-meta-book
فیلد 3 شناسه: publisher-meta-book
```

---

### 5. متای ترم (Term Meta)

برای دسته‌بندی‌ها و تگ‌ها:

#### 5.1. با ID ثابت:
```
شناسه فیلد: icon-meta-term-10
شناسه فیلد: color-meta-term-20
```

#### 5.2. با ID داینامیک:
```
فیلد 1 شناسه: term_id
فیلد 2 شناسه: description-meta-term
```

---

### 6. متای کامنت (Comment Meta)

```
شناسه فیلد: helpful-meta-comment-150
شناسه فیلد: verified-meta-comment-200
```

---

## 💡 مثال‌های کاربردی

### مثال 1: فرم پروفایل کاربری

**نام فرم:** `user-profile-meta`

| برچسب فیلد | شناسه فیلد | مقدار نمونه |
|------------|------------|-------------|
| شماره تماس | `phone-meta-user` | 09123456789 |
| آدرس | `address-meta-user` | تهران، خیابان... |
| کد ملی | `national_code-meta-user` | 0012345678 |
| شغل | `job-meta-user` | برنامه‌نویس |

**نتیجه:** همه این اطلاعات در `user_meta` کاربر فعلی ذخیره می‌شوند.

---

### مثال 2: فرم به‌روزرسانی محصول

**نام فرم:** `product-update-meta`

| برچسب فیلد | شناسه فیلد | نوع فیلد |
|------------|------------|----------|
| آیدی محصول | `product_id` | Hidden |
| تخفیف ویژه | `special_discount-meta-product` | Number |
| گارانتی | `warranty_period-meta-product` | Text |
| موجودی | `available_stock-meta-product` | Number |

**نحوه کار:**
1. کاربر ID محصول را وارد می‌کند (یا از URL می‌خوانید)
2. سایر فیلدها پر می‌شوند
3. سیستم خودکار متاها را در محصول مورد نظر ذخیره می‌کند

---

### مثال 3: فرم ثبت کتاب (Custom Post Type)

**نام فرم:** `book-register-meta`

فرض کنید پست تایپ `book` دارید:

| برچسب فیلد | شناسه فیلد | نوع فیلد |
|------------|------------|----------|
| آیدی کتاب | `book_id` | Number |
| نویسنده | `author_name-meta-book` | Text |
| ژانر | `genre-meta-book` | Select |
| سال انتشار | `publish_year-meta-book` | Number |
| ناشر | `publisher-meta-book` | Text |
| ISBN | `isbn-meta-book` | Text |

---

### مثال 4: فرم نظرات پیشرفته

**نام فرم:** `review-advanced-meta`

| برچسب فیلد | شناسه فیلد |
|------------|------------|
| آیا مفید بود؟ | `is_helpful-meta-comment-{ID}` |
| امتیاز کیفیت | `quality_rating-meta-comment-{ID}` |

---

### مثال 5: فرم چند منظوره

**نام فرم:** `multi-purpose-meta`

```
فیلد 1: phone-meta-user (شماره تماس کاربر)
فیلد 2: product_id (محصول مورد نظر)
فیلد 3: user_review-meta-product (نظر کاربر در متای محصول)
فیلد 4: purchase_count-meta-user (تعداد خرید در متای کاربر)
```

این فرم همزمان در User Meta و Product Meta ذخیره می‌کند!

---

## 📌 نکات مهم

### 1. نام‌گذاری فرم
- حتماً نام فرم باید با `-meta` تمام شود
- ❌ اشتباه: `contact-form`
- ✅ درست: `contact-form-meta`

### 2. شناسه فیلدها
- از خط تیره (`-`) استفاده کنید نه آندرلاین
- ❌ اشتباه: `phone_meta_user`
- ✅ درست: `phone-meta-user`

### 3. کاربر باید لاگین باشد
برای ذخیره در User Meta، کاربر باید لاگین کرده باشد. در غیر این صورت متا ذخیره نمی‌شود.

### 4. ID داینامیک
برای استفاده از ID داینامیک، نام فیلد شامل ID باید یکی از این‌ها باشد:
```
product_id, product-id, productId, productID
post_id, post-id, postId, postID
book_id, book-id, bookId, bookID
term_id, term-id, termId, termID
```

### 5. اولویت پردازش
اگر چند فیلد با الگوی مختلف وجود داشته باشد، سیستم به ترتیب زیر پردازش می‌کند:
1. User Meta
2. Product Meta با ID ثابت
3. Product Meta با ID داینامیک
4. Post Meta
5. پست تایپ دلخواه
6. Term Meta
7. Comment Meta

### 6. امنیت
- تمام داده‌ها با `sanitize_text_field()` پاکسازی می‌شوند
- ID ها با `intval()` تبدیل به عدد صحیح می‌شوند
- کاربران غیرمجاز نمی‌توانند متای دیگران را تغییر دهند

---

## 🔍 عیب‌یابی

### مشکل: متا ذخیره نمی‌شود

**راه‌حل‌های محتمل:**

1. **چک کنید نام فرم با `-meta` تمام شود**
   ```
   ✅ my-form-meta
   ❌ my-form
   ```

2. **چک کنید شناسه فیلد صحیح باشد**
   ```
   ✅ phone-meta-user
   ❌ phone_meta_user
   ❌ phonemetauser
   ```

3. **برای User Meta کاربر باید لاگین باشد**
   ```php
   // چک کردن لاگین بودن
   if (!is_user_logged_in()) {
       echo "کاربر لاگین نیست!";
   }
   ```

4. **برای Product/Post Meta، ID باید معتبر باشد**
   ```php
   // چک کردن وجود محصول
   $product = wc_get_product(123);
   if (!$product) {
       echo "محصول پیدا نشد!";
   }
   ```

5. **دیباگ کردن**
   
   این کد را موقتاً به `functions.php` اضافه کنید:
   ```php
   add_action('dynamic_meta_form_submitted', function($record, $handler) {
       error_log('Form submitted: ' . print_r($record->get('fields'), true));
   }, 10, 2);
   ```
   
   سپس لاگ‌ها را بررسی کنید:
   ```
   مسیر: wp-content/debug.log
   ```

### مشکل: متا چند بار تکرار می‌شود

**علت:** ممکن است فیلتر یا اکشن چند بار اجرا شده باشد.

**راه‌حل:** مطمئن شوید کد را فقط یک‌بار در `functions.php` قرار داده‌اید.

### مشکل: برخی فیلدها ذخیره می‌شوند، برخی نه

**راه‌حل:** 
- نام فیلدها را دقیق چک کنید
- مطمئن شوید پست تایپ یا ترم مورد نظر وجود دارد
- از Developer Tools مرورگر برای بررسی درخواست‌ها استفاده کنید

---

## 🛠️ سفارشی‌سازی

### افزودن اکشن سفارشی پس از ذخیره

```php
add_action('dynamic_meta_form_submitted', function($record, $handler) {
    // کد سفارشی خود را اینجا بنویسید
    // مثلاً ارسال ایمیل، ثبت لاگ، و غیره
    
    $form_name = $record->get_form_settings('form_name');
    $fields = $record->get('fields');
    
    // ارسال ایمیل به ادمین
    wp_mail('admin@site.com', 'فرم ثبت شد', 'یک فرم جدید ثبت شد');
}, 10, 2);
```

### فیلتر کردن داده قبل از ذخیره

کد فعلی را می‌توانید تغییر دهید و فیلتر اضافه کنید:

```php
// قبل از خط update_user_meta
$field_value = apply_filters('dynamic_meta_before_save', $field_value, $field_id);
```

سپس در جای دیگر:

```php
add_filter('dynamic_meta_before_save', function($value, $field_id) {
    if ($field_id === 'phone-meta-user') {
        // فرمت شماره تلفن را تغییر دهید
        $value = str_replace(' ', '', $value);
    }
    return $value;
}, 10, 2);
```

---

## 📊 مثال پیشرفته: فرم ثبت سفارش

```
نام فرم: order-registration-meta

فیلدها:
1. user_phone-meta-user (شماره کاربر)
2. user_address-meta-user (آدرس کاربر)
3. product_id (آیدی محصول - Hidden)
4. customer_note-meta-product (یادداشت مشتری روی محصول)
5. last_purchase_date-meta-product (تاریخ آخرین خرید)
6. term_id (آیدی دسته‌بندی - Hidden)
7. most_sold_in_category-meta-term (پرفروش‌ترین محصول دسته)
```

این فرم همزمان:
- اطلاعات کاربر را به‌روز می‌کند
- متای محصول را تغییر می‌دهد
- آمار دسته‌بندی را ثبت می‌کند

---

## 📞 پشتیبانی

اگر سوالی دارید یا به مشکلی برخوردید:

1. ابتدا بخش [عیب‌یابی](#عیب‌یابی) را مطالعه کنید
2. مطمئن شوید از آخرین نسخه المنتور استفاده می‌کنید
3. دیباگ لاگ وردپرس را فعال کنید
4. فیلدها و تنظیمات فرم را دوباره بررسی کنید

---

## 📝 مجوز

این کد رایگان و آزاد است. می‌توانید آن را در پروژه‌های شخصی و تجاری خود استفاده کنید.

---**
